import { yellow, lightBlue } from "@mui/material/colors";

const colorTheme = {
  palette: {
    primary: lightBlue,
    secondary: yellow,
    chart: {
      gridColor: "#ebebeb",
    },
  },
};

export default colorTheme;
